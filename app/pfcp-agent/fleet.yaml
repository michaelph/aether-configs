name: pfcp-agent
defaultNamespace: tost
helm:
  releaseName: pfcp-agent
  repo: https://charts.aetherproject.org
  chart: sdfabric
  version: 1.1.0
  valuesFiles:
    - values.yaml
dependsOn:
  - name: onos
